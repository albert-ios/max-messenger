# 1. Используем Node.js (легкую версию Alpine)
FROM node:18-alpine

# 2. Создаем рабочую папку внутри контейнера
WORKDIR /app

# 3. Копируем файлы зависимостей
COPY package*.json ./

# 4. Устанавливаем зависимости
RUN npm install

# 5. Копируем исходный код проекта
COPY . .

# 6. Открываем порт 3000
EXPOSE 3000

# 7. Команда запуска
CMD ["npm", "run", "server"]